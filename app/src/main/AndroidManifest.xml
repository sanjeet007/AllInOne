<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ezimax.allinone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="com.ezimax.allinone.LinkedinActivity" />
        <activity android:name="com.ezimax.allinone.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ezimax.allinone.RecyclerViewActivity" />
        <activity android:name="com.ezimax.allinone.YouTubeActivity" />
        <activity android:name="com.ezimax.allinone.TwitterActivity" />
        <activity android:name="com.ezimax.allinone.InstagramActivity" />
        <activity android:name="com.ezimax.allinone.MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>